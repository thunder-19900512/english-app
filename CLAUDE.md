# EnglishApp — Claude向け改善仕様書

## 🔴 最優先の約束：名簿（実名）をコードに書かない

子どもの名前・学年・ホーム番号・A/Bクラスは、**ソースコードにも設定ファイルにも書かない**。
書くとビルドされたJSに含まれ、公開URLから誰でも読める（2026年8月に実際にこの状態になっていた）。

- 名簿の置き場所は **Supabaseの `students` テーブルだけ**。RLSで「あいことば」ログイン済みの人しか読めない
- アプリは `src/data/students.ts` の `loadRoster()` で、ClassGate通過後に取得する
- `STUDENTS` は起動時は空。中身はloadRosterが入れる
- 学園のルール（`子どもの写真・動画・記録をAIで扱うときのルール` 2026-08-20）では、
  **名前を伏せた子どもの記録も、個人アカウントのAIに入れるのはNG**。名簿をこのリポジトリに戻さない

## このアプリについて

小学校英語学習用のWebアプリ。React + TypeScript + Vite で構築。
Supabaseをデータベースとして使用。GitHub Pagesにデプロイ済み。

開発者：山田ゆうじ（プログラミング初心者・非エンジニア）
- 専門用語をできるだけ避けて説明すること
- 変更前に「何をするか・なぜするか・安全かどうか」を説明すること
- 大きな変更は一度に全部やらず、段階的に確認しながら進めること

---

## いまの仕組み（2026-09-24 時点）

- **フロントエンド**: React 19 + TypeScript + Vite。公開は GitHub Pages（`npm run deploy`）
- **DB**: Supabase（プロジェクト「English Learning Adventure」）。ログインは「あいことば」1つのクラス共用アカウント
- **AI・音声のキーはブラウザに置かない**。Supabase の Edge Function（`supabase/functions/`）が中継する
  - `gemini` … AI英会話・お話づくり（アプリ側は `src/lib/aiProxy.ts` から呼ぶ）
  - `azure-token` … 発音チェック用の10分で切れるトークン
  - `staff-check` … スタッフPINの照合（PINは `staff_secret` テーブル。コードに書かない）
  - キーは Supabase Secrets（`GEMINI_API_KEY` ほか）。値は山田が管理画面で入れる
- クラス全体の1日の上限は `api_usage` ＋ `consume_api_quota`。端末ごとの上限（localStorage）も併用
- なりすまし対策の応急処置：ポイントを使うときの合言葉（`spend_pins` ＋ `set/check/reset_spend_pin`）。
  本命は学園アカウントでのGoogleログイン（未着手・学園の情報担当に確認中）
- `.env` には Supabase のURLと公開用の anon キーだけ（`.gitignore` 済み）

## セキュリティの約束

- キー・PIN・子どもの名前は、コードにもチャットにも書かない
- push の前に、キーらしい文字列や名簿が入っていないか確認する

## ポイント設計ルール（新モードを追加するときは必ず準拠）

チート（＝まっとうに学ばずに点を稼ぐ）を防ぐため、加点は必ず次を守る：

1. **加点は「まっとうな学習の成果」にひもづける**。単に「押した／完了した」だけでは点を出さない。
   - クイズ系は**正答率でスケール**し、半分未満なら加点なし（例：`addPoints(key, { multiplier: correctRatio })`、`ratio < 0.5` は加点しない）。
   - 選択式で「間違えると答えが浮き出る」ヒントがある場合、**一発正解だけを加点対象にカウント**（ヒントで正解しても加点は減る）。
   - 発音・音読系は合格ライン（accuracyScore）を満たしたときだけ。
2. **繰り返すほど加点は逓減し、最後は0**。これは `usePoints.addPoints` が全モード共通で処理する
   （`20→10→5→2→1→0`）。新モードも必ず `addPoints(activityKey, ...)` 経由にすること。
   同じ活動の連打で無限に稼げないのが前提。
3. **失敗（不合格）は clearCounts を増やさない**（＝逓減カウンタを消費しない）。合格したクリアだけ数える。
4. **ポイントは累計（points）＝単調増加。買い物でpointsから引かない**。使用は shop.spent／
   寄付は shop.donated（どちらも単調増加＝maxマージ）への加算で表す。**残高 = points − spent − donated**。
   ショップ・みんなの木は「消費のみ」で加点はしない。
5. **「広く浅く」より「1つを深く」が得になるようにする**（2026-09-11）。辞書は単元名がキーに入るため
   単元を変えれば毎回1回目＝20Pになり、1コマで20単元を回すのが最適解になっていた。
   `src/lib/dictDepth.ts` で、同じ単元の別モードをクリア済みなら上乗せ（×1.25／×1.5）、
   今日3つ目以降の新しい単元は減額（×0.75／×0.5。2026-09-16にゆるめた）。
   **子どもには「減る」と宣告しない**：辞書トップで「今日あと◯単元まんてん」を先に見せ、減ったときは「深めるほうが大きい」と次の一手で伝える（「最悪」「撤回しろ」の声が来た）。新しい「単元×モード」型の活動も同じ考えで設計する。
6. **DB側にも見張りがある**：`students` の points は、同じ更新で clear_counts が増えた回数×70 を超えて
   増やせない（トリガー `guard_points_increase`）。localStorageを書き換えてもサーバで差し戻される。
   先生の手動加算は clear_counts.teacher_bonus を一緒に増やして通す。

7. **久しぶりの復習は、また加点する**（2026-09-16）。最後のクリアから1週間あくごとに逓減を2段階もどす
   （`clearAt_{studentId}` に活動キーごとの最終クリア時刻を持つ。端末内のみ・同期しない）。
   日をまたげば逓減しきっていても1Pは入る（同じ日の連打には効かない）。
   連打では回復しないので荒稼ぎにはならない。ふりかえり（毎日3〜8P固定）より、やり切った単元の
   練習が軽くなる逆転を直すための仕組み。

## 作業開始前に必ずやること

1. `src/` フォルダの構成を確認する
2. 変更を加える前に必ず山田に説明・確認を取る（何を・なぜ・安全か）

## 文言のルール（子ども向け画面）

対象は小学5・6年生。**小学校で習う漢字は開かない（漢字で書く）**。

- 漢字にする：感想／結果／名前／声／音／背景／表示／紹介／準備／失敗／通信／確認／
  最新／全部／合計／以上／単語／文字／記録／必要／届く／伝える／減る／残る／試す／
  選ぶ／送る／使う／始める／開く／変える／違う／消す／持つ／枚／困る／混む
- **ひらがなのままにする**：中学で習う漢字を含む語
  （もどる・貼る・誰・ためた・さしこみ・はらう など）。ふりがなは付けない
  - 例外：**機能名は山田の判断が優先**。「着せ替え」は「替」が中学漢字だが漢字で書く
    （2026-09-09 決定）。ほかの機能名は「お話づくり」「ふりかえり」
- 表記のゆれを作らない：
  - 「読み込み中…」「確認中…」（"よみこみちゅう"等の別表記を作らない）
  - 「もう一度」（「もう一回」は使わない）
  - 三点リーダは `…`（`...` にしない）
- 語尾の親しみ（「〜しよう」「〜だよ」）は残してよい。幼くしすぎない
- **学習内容は対象外**：単語の意味（vocabulary.ts）、ダイアログの訳（dialogueData.ts）、
  クイズの問題・選択肢（*QuizData.ts）、フォニックスの説明（stages.ts）は
  教材としての表記があるので、この規則で一括置換しない

## 最近の更新（トップ画面）

子どもに見える変更をデプロイしたら、`src/data/updates.ts` の先頭に1行足す（子どもの言葉で・名前は書かない）。
📮の声から生まれたものは `fromVoice: true`。「送った声が反映されたか知りたい」への返事の代わり（2026-09-13）。
